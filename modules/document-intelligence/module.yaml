module:
  id: document-intelligence
  name: document-intelligence
  version: 0.1.0
  state: disabled_stale_scaffold
  cost_mode: zero_spend
  enabled: false
  disabled_reason: "Placeholder module is not production source. Keep disabled until replaced by verified implementation."
  requires_approval_for:
    - external_write
    - paid_call
    - production_change
  healthcheck: null
