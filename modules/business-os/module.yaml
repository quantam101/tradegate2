module:
  id: business-os
  name: business-os
  version: 0.1.0
  state: scaffold
  cost_mode: zero_spend
  enabled: true
  requires_approval_for:
    - external_write
    - paid_call
    - production_change
  healthcheck: /api/health/business-os
